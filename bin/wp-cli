#!/bin/bash -e

DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && cd .. && pwd )

export LD_LIBRARY_PATH=${DIR}/php/lib

${DIR}/php/bin/php -c ${SNAP_COMMON}/config/php.ini ${DIR}/bin/wp-cli.phar "$@"

