#!/bin/bash -e

DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && cd .. && pwd )

export WP_CONFIG_DIR=${SNAP_COMMON}/config/wordpress

sudo -H -E -u wordpress LD_LIBRARY_PATH=${DIR}/php/lib ${DIR}/php/bin/php -c ${SNAP_COMMON}/config/php.ini ${DIR}/bin/wp-cli.phar --path=${SNAP}/wordpress "$@"
